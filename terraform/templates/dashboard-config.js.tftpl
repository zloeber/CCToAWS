window.CCT_DASHBOARD_CONFIG = {
  region: "${aws_region}",
  userPoolId: "${cognito_user_pool_id}",
  clientId: "${cognito_client_id}",
  hostedUiDomain: "${cognito_hosted_ui_domain}",
  apiBaseUrl: "${http_api_endpoint}",
  redirectUri: "${dashboard_redirect_uri}",
};
